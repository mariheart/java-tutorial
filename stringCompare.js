
let firstString = "First String";
let secondString = "Second String";

function equalStatements (firstString, secondString) {
    if (firstString == secondString) {
        alert("That's....that's plagiarism, bro.")
    } else {
        alert("You're cool.")
    }
}

equalStatements(firstString, secondString);